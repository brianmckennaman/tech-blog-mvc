{{#each posts as |post| }}
<div class="row md-4 post">
    <div class="col-md-5">
        <h1>
            <a href="/post/{{post_id}}"></a>
        </h1>
        <p>{{post_body}}</p>
    </div>
</div>